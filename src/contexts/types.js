import PropTypes from "prop-types";


export const TAuthUserProvider = {
  children: PropTypes.node.isRequired,
}

export const TLocaleProvider = {
  children: PropTypes.node.isRequired,
}
export const TColorModeProvider = {
  children: PropTypes.node.isRequired,
}